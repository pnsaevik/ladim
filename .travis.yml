language: python

python:
- "3.6"
- "3.7"
- "3.8"
- "3.9"
- "3.10"

before_install:
  - python --version
  - pip --version
  - pip install --upgrade setuptools numpy pip

install:
  - pip install -v .
  - pip list

script:
  - ladim --help
